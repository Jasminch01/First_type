//==> interface
function drowRactangle(options) {
    var width = options.width;
    var height = options.height;
}
// ---> get suggestion call the drowreactangle function calling
drowRactangle({
    width: 30,
    height: 20,
});
