"use strict";
// => build in types : any,string, boolean, void, object, undifind, null
let userId;
let userFirstName;
let userLastName;
let isTrue;
userId = 1001;
userFirstName = "user1";
userLastName = 'kxy';
isTrue = false;
console.log(`user is ${userId}, user name : ${userFirstName.concat(userLastName)}, user role is admin ${isTrue}`);
